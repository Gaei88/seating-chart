---
name: 排排座
description: 当要制作、打开或编辑「排座 / 座位表 / 排排座 / 安排座位 / 谁坐哪 / 婚宴座位 / 会议座位 / 圆桌座位 / 答辩座位 / 餐厅排位 / 分享座位链接 / seating chart / seating plan」等交互式座位安排时启用。本 skill 提供一个自包含的交互式座位表网页（assets/seating.html），支持拖拽换座、增删人员与桌位、按名单导入（xlsx/csv）、颜色标记、打印、CSV 导出，以及一键「分享」生成内嵌数据的公开链接（数据写在 URL 的 #s= 片段，纯前端、无需服务器）。
version: 1.0.0
author: kaisa
level: personal
metadata:
  csig:
    spec_version: "V1.0"
    data_classification: L1
    lifecycle: released
---

# 排排座（交互式座位表）

## 用途

生成一个可直接在浏览器里交互编辑的座位安排网页，用于婚宴、会议、聚餐、答辩、活动入座等场景。产出是单个自包含 HTML 文件（无需联网、无需后端），双击即可打开，所有改动自动存到浏览器 localStorage。

## 触发场景

- 用户说「排个座」「做个座位表」「排排座」「安排一下谁坐哪」「婚宴座位怎么排」「会议座位表」等。
- 用户想打开 / 编辑此前用本 skill 生成的座位表。

## 调用步骤

1. 定位本 skill 目录：优先用变量 `${CODEBUDDY_SKILL_DIR}`；若该变量为空，则从 Skill 工具返回的 location 路径确定（即包含本 SKILL.md 的目录）。
2. 复制模板到当前工作区（保留一份可编辑副本，避免覆盖 skill 源文件）：
   - 源文件：`<skill-dir>/assets/seating.html`
   - 目标：当前 workspace 下，例如 `<workspace>/排排座.html`，或带时间戳的子目录（如 `<workspace>/2026-<MM>-<DD>-座位表/排排座.html`）。
3. 用 `present_files` 打开目标 HTML（会自动在预览面板内渲染，用户可直接拖拽编辑）。
4. 简短说明能力：拖拽换位、增删人/桌、右键填色/删除、导入 xlsx·csv 名单、打印、导出 CSV、一键「分享」生成可群发的公开链接；并提醒数据存于当前浏览器本地（换浏览器或清缓存会丢失，重要排座建议导出 CSV 备份）。

## 分享链接说明

- 工具栏「🔗 分享」会把当前所有排座（姓名、桌号、颜色、字号等）编码进 URL 的 `#s=` 片段并复制到剪贴板；该链接打开即还原排座，且对方还能继续编辑。
- 链接纯前端、无需后端存储，但较长（人数多时 URL 偏长）；超大排座建议改用 CSV 导出备份。
- 若要把本地预览里的当前排座变成可群发的公开链接：将本地预览「分享」得到的 `#s=...` 片段，接到已部署的公开主页之后即可（部署目录见 `seating-deploy/`）。
- 部署为公开站点用 `workbuddy_cloudstudio_deploy`（目录含 `index.html`），返回 `shareLink` 即分享链接。

## 注意事项

- 不要把数据再次写回 `assets/seating.html`（那是模板，保持纯净）。
- 若用户要给某次具体活动排座，复制出带日期的副本更便于区分多份排座。
- 名单导入约定：第 1 列为姓名、第 2 列（可选）为桌号、第 3 列（可选）为性别、第 4 列（可选）为职级。导入后左侧人员名单栏以彩色徽标显示性别/职级，右侧座位图仅显示姓名（鼠标悬停座位可看性别职级）；导出 CSV 也含性别、职级两列。
