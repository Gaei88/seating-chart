---
name: 排排座
description: 当要制作、打开或编辑「排座 / 座位表 / 排排座 / 安排座位 / 谁坐哪 / 婚宴座位 / 会议座位 / 圆桌座位 / 答辩座位 / 餐厅排位 / 智能排座 / 分享座位链接 / seating chart / seating plan」等交互式座位安排时启用。本 skill 提供一个自包含的交互式座位表网页（assets/seating.html）：支持拖拽换座、增删人员与桌位、按名单导入（xlsx/csv，含公司/部门/职级/性别）、下载导入模板、一键「智能排座」（职级靠前、同公司部门打散、每桌性别均衡）、颜色标记、打印、CSV 导出，以及一键「分享」生成内嵌数据的公开链接（数据写在 URL 的 #s= 片段，纯前端、无需服务器）。
version: 1.1.0
author: kaisa
level: personal
metadata:
  csig:
    spec_version: "V1.0"
    data_classification: L1
    lifecycle: released
---

# 排排座（交互式座位表）

## 用途

生成一个可直接在浏览器里交互编辑的座位安排网页，用于婚宴、会议、聚餐、答辩、活动入座等场景。产出是单个自包含 HTML 文件（无需联网、无需后端），双击即可打开，所有改动自动存到浏览器 localStorage。

## 触发场景

- 用户说「排个座」「做个座位表」「排排座」「安排一下谁坐哪」「婚宴座位怎么排」「会议座位表」「自动排一下座」等。
- 用户想打开 / 编辑此前用本 skill 生成的座位表。

## 调用步骤

1. 定位本 skill 目录：优先用变量 `${CODEBUDDY_SKILL_DIR}`；若该变量为空，则从 Skill 工具返回的 location 路径确定（即包含本 SKILL.md 的目录）。
2. 复制模板到当前工作区（保留一份可编辑副本，避免覆盖 skill 源文件）：
   - 源文件：`<skill-dir>/assets/seating.html`
   - 目标：当前 workspace 下，例如 `<workspace>/排排座.html`，或带时间戳的子目录（如 `<workspace>/2026-<MM>-<DD>-座位表/排排座.html`）。
3. 用 `present_files` 打开目标 HTML（会自动在预览面板内渲染，用户可直接拖拽编辑）。
4. 简短说明能力：拖拽换位、增删人/桌、右键编辑信息/填色/删除、导入 xlsx·csv 名单（可先下载模板）、**智能排座**、打印、导出 CSV、一键「分享」生成可群发的公开链接；并提醒数据存于当前浏览器本地（换浏览器或清缓存会丢失，重要排座建议导出 CSV 备份）。

## 名单字段

导入 / 导出均支持 6～7 列，除姓名外全部可选：

| 列 | 字段 | 说明 |
|---|---|---|
| 1 | 姓名 | 必填 |
| 2 | 桌号 | 可选，如「第1桌 / 1 / 第一桌」；留空进「新桌」 |
| 3 | 性别 | 可选，男 / 女（也认 M / F / male / female / 先生 / 女士） |
| 4 | 职级 | 可选，如 总监 / 经理 / P7 / M2 / 实习生 |
| 5 | 公司 | 可选，智能排座时按公司打散 |
| 6 | 部门 | 可选，智能排座时按部门打散 |

- **带表头的文件会按表头名称自动识别列**，列顺序与模板不一致也能导入；无表头则按上表默认顺序读取。
- 导入弹窗里可直接下载 **Excel / CSV 模板**。
- 座位上左键或右键 →「✏️ 编辑信息」可随时补改姓名、性别、公司、部门、职级。

## 智能排座（🎯 按钮）

按三条大原则自动分配座位，可单独开关：

1. **职级高的坐前面几桌** —— 按职级从高到低，高职级优先落第 1 桌起的前几桌。
2. **同公司 / 同部门打散** —— 同一公司或部门的人均匀分布到各桌，避免扎堆。
3. **每桌性别均衡** —— 各桌男女比例接近整体比例，并尽量保证每桌至少 1 位女性。

使用要点：

- 弹窗里设「桌数」和「每桌人数上限」，两者乘积必须 ≥ 总人数。
- **职级高低顺序**以文本框列出（越靠上越资深），自动按内置职级词典排序。遇到「P7」「M2」或公司自定义职级时，请让用户确认或手动调整顺序——这直接决定谁坐前面。
- 排完会给出每桌的人数 / 男女 / 公司分布 / 职级均分体检表；**点「取消」可撤销**并恢复排座前的安排。
- 排座不是终点，之后仍可手动拖拽微调。

## 分享链接说明

- 工具栏「🔗 分享」会把当前所有排座（姓名、桌号、颜色、字号等）编码进 URL 的 `#s=` 片段并复制到剪贴板；该链接打开即还原排座，且对方还能继续编辑。
- 链接纯前端、无需后端存储，但较长（人数多时 URL 偏长）；超大排座建议改用 CSV 导出备份。
- 若要把本地预览里的当前排座变成可群发的公开链接：将本地预览「分享」得到的 `#s=...` 片段，接到已部署的公开主页之后即可（部署目录见 `docs/`）。
- 部署为公开站点用 `workbuddy_cloudstudio_deploy`（目录含 `index.html`），返回 `shareLink` 即分享链接。

## 注意事项

- 不要把数据再次写回 `assets/seating.html`（那是模板，保持纯净）。
- 若用户要给某次具体活动排座，复制出带日期的副本更便于区分多份排座。
- 导入 Excel 需联网加载 CDN 上的 SheetJS；离线环境请改用 CSV。
- 智能排座依赖名单质量：**没填职级就分不出前后，没填性别就做不了性别均衡，没填公司/部门就打散不了**。导入后先确认这几列齐全再点排座。
- 女性总人数少于桌数时无法满足「每桌都有女性」，结果弹窗会给出告警，需人工调整。
- 数据存于当前浏览器本地，换浏览器或清缓存会丢失，重要排座请导出 CSV 备份。
